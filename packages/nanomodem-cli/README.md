# nanomodem-cli

Command-line interface for nanomodem acoustic modems.

## Install

```bash
pipx install "git+https://github.com/foxpoint-se/nanomodem-python.git#subdirectory=packages/nanomodem-cli"
```

## Development

From the repo root:

```bash
make install
uv run nanomodem --help
```
